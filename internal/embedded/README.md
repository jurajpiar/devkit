# Embedded Binaries

This directory contains embedded binaries for the egress proxy.

The files `egressproxy-linux-amd64` and `egressproxy-linux-arm64` are generated
by running `make build` or `scripts/build.sh`.

These are placeholder files if you're building without the full build script.
